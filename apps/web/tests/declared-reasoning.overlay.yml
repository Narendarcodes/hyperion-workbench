# This scenario starts the default on its own declared reasoning model
# so the effort pane describes that model from the first open.
- id: agent-default-model
  config:
    provider: acme-gateway
    model: acme-think
