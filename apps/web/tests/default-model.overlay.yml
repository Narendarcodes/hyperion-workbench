# This scenario declares its own pi-ai routes and starts the default on
# one of them: the switch under test needs somewhere to start and
# somewhere to go, independent of the shipped local default.
- id: agent-default-model
  config:
    provider: origin-gateway
    model: origin-large
